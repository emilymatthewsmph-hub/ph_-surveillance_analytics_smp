# src/generate_synthetic_data.py

import os
import numpy as np
import pandas as pd

OUTPUT_DIR = "data"
N_ROWS = 5000

def generate_ed_visits(n_rows: int) -> pd.DataFrame:
    np.random.seed(42)

    counties = ["CountyA", "CountyB", "CountyC"]
    ages = np.random.randint(0, 90, size=n_rows)
    sex = np.random.choice(["F", "M"], size=n_rows)
    county = np.random.choice(counties, size=n_rows)

    # Simple date range
    dates = pd.date_range("2024-01-01", "2024-12-31", freq="D")
    visit_dates = np.random.choice(dates, size=n_rows)

    # Very simple chief complaints / dx codes
    chief_complaints = np.random.choice(
        ["cough and fever", "shortness of breath", "sore throat", "abdominal pain"],
        size=n_rows
    )
    dx_codes = np.random.choice(
        ["J10", "J11", "U07.1", "J21", "R50", "R05"],
        size=n_rows
    )

    df = pd.DataFrame({
        "visit_id": np.arange(1, n_rows + 1),
        "patient_id": np.random.randint(1, n_rows // 2 + 1, size=n_rows),
        "age": ages,
        "sex": sex,
        "county": county,
        "chief_complaint": chief_complaints,
        "diagnosis_code": dx_codes,
        "visit_date": visit_dates
    })

    return df

def main():
    os.makedirs(OUTPUT_DIR, exist_ok=True)
    ed_visits = generate_ed_visits(N_ROWS)
    out_path = os.path.join(OUTPUT_DIR, "ed_visits.csv")
    ed_visits.to_csv(out_path, index=False)
    print(f"ED visits written to: {out_path}")

if __name__ == "__main__":
    main()
